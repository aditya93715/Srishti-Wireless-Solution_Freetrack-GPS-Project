import React from 'react';
import PlaceholderPage from '../../../components/PlaceholderPage';

const VehicleConfig = () => {
  return (
    <PlaceholderPage 
      title="Vehicle Configuration" 
      icon="vehicle-config"
      accent="#00c278"
      description="Vehicle Configuration module for fleet management system. Configure and manage your Vehicle Configuration settings here."
    />
  );
};

export default VehicleConfig;
